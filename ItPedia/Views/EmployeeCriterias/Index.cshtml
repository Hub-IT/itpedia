@model IEnumerable<ItPedia.Models.EmployeeCriteria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IndustryCriteria.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IndustryCriteria.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeCriteriaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeCriteriaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeCriteriaId })
        </td>
    </tr>
}

</table>
