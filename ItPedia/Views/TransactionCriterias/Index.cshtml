@model IEnumerable<ItPedia.Models.TransactionCriteria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PerMonth)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PerMonth)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransactionCriteriaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransactionCriteriaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransactionCriteriaId })
        </td>
    </tr>
}

</table>
