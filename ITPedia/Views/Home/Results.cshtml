@using ITPedia.Models
@{
    ViewBag.Title = "Results";
}

<h2>@ViewBag.YouSelected2</h2>

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            <img alt="Storage" height="20" width="20" src="/Content/img/kattekrab-mainframe.png"/> Hardware
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.HardwareSolutions)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            <img alt="Storage" height="20" width="20" src="/Content/img/software.png"/> Software
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.SoftwareSolutions)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            <img alt="Storage" height="20" width="20" src="/Content/img/applications.png"/> Applications
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.AppSolutions)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

</div>

<div class="row">
    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            <img alt="Storage" height="20" width="20" src="/Content/img/network.png"/> Networks
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.NetSolutions)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            <img alt="Storage" height="20" width="20" src="/Content/img/storage.png"/> Storage
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.StorageSolutions)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

</div>

<hr/>

<div class="row">
    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
             Fixed Costs
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.FixedCosts)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>

    <div class="list-group col-md-4 col-sm-6 col-xs-12">
        <p class="list-group-item active">
            RecurringCosts
        </p>

        @foreach (KeyValuePair<Term, string> entry in ViewBag.Solutions.RecurringCosts)
        {
            @Html.ActionLink(entry.Value + " " + entry.Key.Name, "Term", "Dictionary", new {id = entry.Key.Name},
                new {@class = "list-group-item"})
        }
    </div>
</div>